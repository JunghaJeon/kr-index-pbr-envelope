# 이 프로젝트에서 일하는 방식

## 지시를 그대로 만들 것

사용자가 파라미터를 지정하면 **그 값으로 만든다.** 지정에는 이유가 있다.

2026-08-01, "PBR 20,20 엔벨로프"를 요청받고 ±20%가 통계적으로 넓다는 이유로
±5/±10 선택 기능을 임의로 추가했다. 사용자는 20%를 봐야 할 이유가 있었고,
정작 원한 것은 **드래그 구간 확대**였다. 요청하지 않은 기능을 넣느라 요청받은
기능을 못 만든 셈이다.

- 데이터에 관찰할 점이 있으면 **말로 한 번만** 하고, 만드는 건 지시대로 만든다.
- 같은 지적을 채팅·UI 문구·주석에 반복해 넣지 않는다. 한 번이면 충분하다.
- 기능을 추가하고 싶으면 만들지 말고 **먼저 묻는다.**

---

# PBR 엔벨로프 차트 (`pbr_chart.html`)

아티팩트: https://claude.ai/code/artifact/c407356e-6bb3-455c-b6f8-2c17bd4d318b
재작업 시 이 URL 로 갱신한다(새로 만들면 링크가 바뀐다).
(2026-08-02 재발행. 이전 URL e4fca015… 는 삭제돼 있어 갱신이 불가능했다.)

## 확정된 사양 — 다시 논의하지 말 것

- 코스피·코스닥 **각각 별도 차트**, 세로로 스택.
- **20일 단순이동평균 ±20% 고정.** 폭 선택 UI 는 넣지 않는다.
- x축은 두 차트가 **공유**한다. 크로스헤어가 날짜로 동기화돼야 같은 시점을 비교할 수 있다.
  (코스닥은 2005-10 부터라 왼쪽이 비지만 정상이다.)
- **가로 드래그로 구간 확대**, 확대 중에는 되돌아갈 **버튼을 화면에 띄운다.**
- **클릭하면 그 날짜의 값이 전부** 표로 나온다 — PBR / 이평 / 상단 / 하단 / 이평대비 /
  지수 / PER / 배당% / 역사 분위.

## 이미 밟은 지뢰 — 같은 실수 금지

**변수명 일괄 치환은 스코프를 깨뜨린다.**
`state.w` → `W` 로 전역 치환했더니 `draw()` 안의 캔버스 너비 `W`(≈946)에 가려져
밴드가 `이평 × (1 ± 946)` 이 됐다. y축이 폭발해 PBR·이평이 납작한 직선으로 보였다.
엔벨로프 폭 상수는 `ENV` 로 둔다. 치환 전에 같은 이름이 다른 스코프에 있는지 본다.

**드래그 판정은 반드시 픽셀 기준.**
인덱스 기준(3일)으로 뒀더니, 전체 구간에서 1px ≈ 7거래일이라 **손떨림 1px 이 확대**로
잡혔다. 되돌리려 클릭할 때마다 다시 확대돼 빠져나올 수 없었다. `DRAG_PX = 6`.

**되돌리기는 숨은 제스처로 만들지 않는다.**
더블클릭 리셋은 코드상 동작했지만 사용자가 찾지 못했다. 확대 상태에서
`2011-12-09 ~ 2017-12-21 ✕` 버튼을 눈에 보이게 띄운다.

## 검증 — 표만 보면 안 된다

위 스케일 버그는 **표 값이 정상이었다**(그 코드가 `draw()` 밖이라 진짜 0.20 을 썼다).
DOM 만 확인해서 놓쳤다. 캔버스는 **픽셀을 직접 읽어** 검증한다:

```js
// 선이 실제 곡선인지 — 세로 분포가 캔버스 높이의 상당 부분이어야 한다
const d = ctx.getImageData(0,0,cv.width,cv.height).data;
// 시리즈 색과 일치하는 픽셀의 min/max y 를 구해 spread / canvasH 를 본다
```
정상값 기준: PBR 선 0.5~0.7, 이평선 0.4 내외. 0.05 이하면 스케일이 깨진 것이다.

상호작용은 `dispatchEvent` 로 합성해 확인한다. 최소한 이 네 가지:
① 2px 손떨림 클릭 → 확대 안 됨 ② 실제 드래그 → 확대 + 리셋 버튼 노출
③ 확대 상태 손떨림 → 추가 확대 안 됨 ④ 리셋 → 전체 복귀

숫자는 **pandas 로 교차검증**한다(이동평균·분위수 특히).

---

# 차트/HTML 아티팩트 공통

렌더링 확인 과정에서 시간을 가장 많이 버렸다. 아래대로 하면 왕복이 확 준다.

## 미리보기 서버부터 띄우고 시작

`file://` 은 Playwright·Browser pane 양쪽에서 **차단**된다. 처음부터 HTTP 로 띄운다.
`python3 -m http.server` 는 charset 헤더를 안 보내 **한글이 깨진다**:

```python
import http.server, socketserver
class H(http.server.SimpleHTTPRequestHandler):
    def guess_type(self, path):
        t = super().guess_type(path)
        return t + "; charset=utf-8" if t == "text/html" else t
socketserver.TCPServer.allow_reuse_address = True
socketserver.TCPServer(("127.0.0.1", 8732), H).serve_forever()
```

## 확인은 Playwright `browser_evaluate` 로

- **Browser pane 의 `navigate` 가 300초 그대로 행에 걸리는 일이 있다.** 매달리지 말고
  Playwright 로 넘어간다. 프로젝트 폴더 밖 파일은 pane 에서 정적 스냅샷(JS 미실행)이고,
  `127.0.0.1` 은 정책상 거부돼 `localhost` 만 열린다 — 제약이 많다.
- Playwright `browser_take_screenshot` 은 무거운 canvas 에서 **5초 타임아웃**으로 거의
  항상 실패한다. 그림이 꼭 필요할 때만 pane 의 `computer{screenshot}` 을 시도한다.
- `browser_evaluate` 로 DOM 텍스트·캔버스 픽셀·합성 이벤트를 모두 확인할 수 있다.
  이게 가장 빠르고 확실하다.
- **`node -e "new Function(src)"` 는 문법만 잡는다.** 실제로 페이지를 띄워야
  `ReferenceError`(함수명 변경 후 호출부 누락 등)가 잡힌다. 반드시 한 번 로드한다.

## 만드는 방식

- 데이터가 큰 페이지는 템플릿에 `__DATA__` 플레이스홀더를 두고 빌드 때 주입한다.
  템플릿만 고쳐 재빌드하면 되므로 반복이 싸다.
- 수천 점 이상 시계열은 SVG 말고 **canvas**.
- 이동평균은 **전 구간에서 한 번** 계산하고 표시할 때 자른다. 보이는 구간만으로
  계산하면 왼쪽 끝 값이 틀린다.

---

# 데이터 (`kr_index_pbr.py`)

- 출처는 KRX 정보데이터시스템이며 **로그인이 필수**다. 자격증명은 `~/.krx.env`
  (`krx_setup.sh` 가 생성, 권한 600). 공개 API 가 아니라 웹 내부 엔드포인트라
  KRX 가 화면을 바꾸면 깨질 수 있다.
- KRX 는 미산출 구간의 PER/PBR 을 **0 으로 채워 내려준다.** 결측 처리 필수.
  초기 구간뿐 아니라 2007-03-13 처럼 정상 구간 한복판에도 있다.
- PBR 유효 시작: KOSPI 2002-04-23, KOSDAQ 2005-10-04.
- 기간 조회는 시작·종료일을 함께 받으므로 **24년치도 요청 3번**이면 끝난다.
  날짜별 루프는 절대 돌리지 않는다.
- 실행: `./.venv/bin/python kr_index_pbr.py` (증분) / 인자로 시작일 주면 재수집.
